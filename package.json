{
  "name": "@trabian-banking/root",
  "private": true,
  "workspaces": ["packages/*"],
  "author": "Matt Dean <matt@trabian.com>",
  "scripts": {
    "build:server":
      "cd packages/mock-data-generator && yarn build && cd ../graphql-types && yarn build && cd ../graphql-mock-schema && yarn build:server"
  }
}
